package ZadaniaDomoweSQL.attributes;

public class PersonUtils {

    public static void printNameWithAge(Person p) {
        String info = p.name + " ma " + p.age;
        System.out.println(info);
    }
}
